#include <stdio.h>
#include <stdlib.h>
int main(void)
{
      float a,b,c;
      printf("dessa vez vamos tentar fazer o sucessor e o antecessor de um numero \n");
      scanf("%f", &a);
      b=a+1;
      c=a-1;
      printf("o numero antecessor de %f eh %f e o sucessor eh %f", a, c, b);
      system("pause");
      } 
