#include <stdio.h>
#include <stdlib.h>
int main(void)
{
      float a,b;
      printf("escolha o numero que quer o quadrado de \n");
      scanf("%f", &a);
      b=a*a;
      printf("o quadrado do numero %f, eh o numero %f \n",a,b);
      system("pause");
      return 0;
      }
